Round avatar for testimonials and team sections. Shows initials on a pine tint when no image.

```jsx
<Avatar name="Maria Alvarez" src="/photo.jpg" size="lg" />
<Avatar name="Jakob Meyer" />
```

Sizes: `sm | md | lg | xl`. Always pass `name` for accessibility + the initials fallback.

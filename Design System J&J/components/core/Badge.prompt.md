Small pill label for status, categories, or metadata.

```jsx
<Badge variant="brand">Web design</Badge>
<Badge variant="success" dot>Live</Badge>
<Badge variant="accent" size="sm">New</Badge>
```

Variants: `neutral | brand | accent | success | warning | danger`. Add `dot` for a status indicator. Keep label text to 1–2 words.

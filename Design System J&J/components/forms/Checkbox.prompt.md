Checkbox with label for consent and opt-ins.

```jsx
<Checkbox label="Yes, send me occasional tips for my site." defaultChecked />
<Checkbox label="I agree to the terms." />
```

Props: `label` plus native checkbox attributes (`checked`, `defaultChecked`, `onChange`, `disabled`).

Multi-line field for messages and project details.

```jsx
<Textarea label="Tell us about your project" rows={5}
  placeholder="What does your business do, and what's the website for?" />
```

Props: `label`, `hint`, `error`, `rows`, plus native `<textarea>` attributes.
